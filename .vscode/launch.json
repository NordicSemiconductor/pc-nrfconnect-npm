{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug: App",
            "type": "chrome",
            "request": "attach",
            "port": 9223,
            "webRoot": "${workspaceFolder}",
            "timeout": 3000
        }
    ],

  }